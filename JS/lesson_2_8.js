    "use strict"
    // Урок 8. Логические операции

    // 1) Самостоятельно вычислите результат следующего выражения: (5 < 6 || (true && (5 >= 5 && (false || true) && (true && true))))
    // 2) Выведите результат выражения в консоль и убедитесь, что в первом пункте вы вычислилии все правильно

    let example = (5 < 6 || (true && (5 >= 5 && (false || true) && (true && true)))); // true

    console.log(example);